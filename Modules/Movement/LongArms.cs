using GorillaLocomotion;
using UnityEngine;

namespace MonkeHavoc.Modules.Movement
{
    public class LongArms
    {
        public static void FOREVERTOGETHER()
        {
            GTPlayer.Instance.transform.localScale = new Vector3(1.25f, 1.25f, 1.25f);
        }

        public static void HELEFTNOOOOOOOOO()
        {
            GTPlayer.Instance.transform.localScale = new Vector3(1f, 1f, 1f);
        }
    }
}